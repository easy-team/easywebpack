{
  "name": "easy-react-common-lib-test",
  "version": "1.0.0",
  "description": "React Project Common Lib Test",
  "scripts": {
    "dev": "easy clean && easy dev",
    "start": "easy build --server",
    "test": "easy build test",
    "build": "easy build prod",
    "deploy": "easy build prod",
    "lint": "eslint .",
    "fix": "eslint --fix .",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s"
  },
  "dependencies": {},
  "devDependencies": {
    "cross-env": "^5.0.5",
    "@easy-team/easywebpack-cli": "^4.0.0",
    "@easy-team/easywebpack-react": "^4.0.0",
    "imagemin-webpack-plugin": "^2.4.2"
  }
}
